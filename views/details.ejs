<%- include ("partials/header") %> 

<div class="container">
    <h1><strong><%= movie.Title %></strong> - <%= movie.Year %> </h1>
    <h6><%= movie.Rated %> | <%= movie.Runtime %> | <%= movie.Genre %> | <%= movie.Released %> </h6>

    <div class="container">
        <img src="<%= movie.Poster %> " alt="<%= movie.Title %> Poster ">
    </div>
</div>

<%- include ("partials/footer") %> 